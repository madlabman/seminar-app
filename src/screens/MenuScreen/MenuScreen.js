import React, {Component} from 'react';
import {View} from 'react-native';
import {Text} from 'react-native-elements';

export default class MenuScreen extends Component {
    render() {
        return (
            <View>
                <Text>Тут будет боковое меню</Text>
            </View>
        )
    }
}